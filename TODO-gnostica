Gnostica-specific task list.
Some numbers carry over from an earlier task-tracking approach.
Mark items done with ✔ + @done(date) as they're confirmed.

✔ #7 - i18n/moveresults work @done(26-08-20)
- #25 - Implement Fool/World/High Priestess major arcana powers using new GameBase
- #29 - reduce test count
- #30 - reduce comments
✔ #31 - board rotation view @done(26-08-20)
- #32 - verify the stale-preview click bug on a real dev server
✔ #38 - tournament rules: turnOrder is now the rank order of the last cards bid and is used by nextPlayer() and redrawOrder in reverse, and cleaned up unnecessary stack variables. @done(26-08-20)
✔ #44 - randomMove() tests and correct pass chat. @done(26-08-20)
✔ #45 - checkX/getPiece crash for virtual chain minions: validateMajorPower() now clones+replays each confirmed step for real (cloneLive(), not clone(), so it sees a click preview's own live board too), same idea as frogger.ts's own validateMove(), but lazy since most Gnostica chains are 0-1 step. @done(26-08-21)
✔ #46 - pyramid overlap in densePieceGrid @done(26-08-20)
- #47 - wire chat() to player names
- #48 - pyramid grid fallback order should depend on the piece's own desired position, not one global order (pieceGridSlots() in gnostica.ts); needs a per-orientation fallback priority list; exact orders TBD.
- #49 - require use/play moves to be meaningful (>=1 power step actually taken), a deliberate break from the literal rules to kill the "you can stop here" messaging on a fully-declined move. Exceptions: declining the TAIL of an already-started major arcana chain stays legal; Fool (uid "00") is fully exempt since a drawn Fool must be played and has no supported power. Giving up a card's power stays via "discard <uid> draw 0", not play-then-decline.
- #50 - refactor Tarot cards
✔ #51 - test separate clusters. @done(26-08-20)
✔ #52 - test double-letter coordinates. @done(26-08-20)
✔ #53 - CellContents.card to uid. @done(26-08-20)
✔ #54 - root cause found: not test-order pollution, a genuine ~1.3% (1/78) chance the random initial deal puts Ace of Rods itself at n0, which forceCardAt's own duplicate-clearing then wipes out from under the push - same failure mode an identical test right above it already guarded against with clearBoard()+explicit forceCardAt(n0). Applied the same fix. @done(26-08-21)
✔ #55 - frame-stepping for multi-step major-arcana moves: render() returns APRenderRep[] (one per step + final) for a genuine 2+-step chain, viewable both live mid-turn (partial preview) and later from history; adopted frogger.ts's _group result-wrapping (required for per-frame annotations, not optional); step separator is now "/" between step segments specifically (pickleMove + the click-driven builders), commas unchanged elsewhere. @done(26-08-21)
✔ #57 - Wheel of Fortune click support: "Cups: new" mode now offers a "Random Card" button (getActionButtons/handleClickCore) whenever pending.opts.allowRandomDraw is set - the only such card - reusing supplyStepCardUid's existing "random" token unmodified. Also fixed a real gap found along the way: applyCups/validateCups's "new" case previously honored the literal string "random" unconditionally, regardless of whether the ACTIVE card's own step actually granted allowRandomDraw - any card's "new" step could be typed by hand to draw randomly. Now gated on opts.allowRandomDraw (already correctly computed per-card by computeShortcutOpts), not the literal token. Also removed a wrong pre-existing rule: createTerritory's random-draw path was enforcing the same spot-card-only restriction the hand-card path has, but per the actual rules there's no restriction on what Wheel of Fortune draws - fixed in powers.ts, hand-card path unaffected. @done(26-08-21)
