Gnostica-specific task list.
Some numbers carry over from a partially-lost task list.
Some items were marked off at transfer here, not at completion.
Mark items done with ✔ + @done(date) as they're confirmed.

✔ #7 - i18n/moveresults work @done(26-08-20)
✔ #25 - Implement Fool/World/High Priestess major arcana powers using new GameBase. @done(26-08-25)
- #29 - reduce test count
- #30 - reduce comments
✔ #31 - board rotation view @done(26-08-20)
✔ #32 - fixed the stale-preview click bug: handleClick() wasn't setting `canrender` on valid-but-incomplete clicks. @done(26-08-22)
✔ #38 - tournament rules: turnOrder now reflects bid rank, used by nextPlayer()/redrawOrder. @done(26-08-20)
✔ #44 - randomMove() tests and correct pass chat. @done(26-08-20)
✔ #45 - checkX/getPiece crash for virtual chain minions fixed. @done(26-08-21)
✔ #46 - pyramid overlap in densePieceGrid fixed. @done(26-08-20)
✔ #47 - gnostica.ts now has a single chatLog() to resolve player names. @done(26-08-21)
✔ #48 - pieceGridSlots()'s fallback order is now determined by the bumped piece's own orientation. @done(26-08-22)
✔ #49 - use/play moves require at least one real power step to be complete (chain-tail declines and Fool/World stay exempt); also closed related gaps. @done(26-08-21)
✔ #49 - follow-up - click-driven step messaging fixed. @done(26-08-22)
✔ #50 - unified MinorCard/MajorCard into one Card class as with Decktet cards. @done(26-08-22)
✔ #51 - test separate clusters. @done(26-08-20)
✔ #52 - test double-letter coordinates. @done(26-08-20)
✔ #53 - CellContents.card to uid. @done(26-08-20)
✔ #54 - fixed a rare random-deal flake (not test-order pollution) in the Ace of Rods push test. @done(26-08-21)
✔ #55 - added frame-stepping: render() returns one frame per step for multi-step major-arcana moves. @done(26-08-21)
✔ #57 - added Wheel of Fortune's Random Card click support, plus two related bugs fixed along the way. @done(26-08-21)
✔ #58 - randomMove: orientation on a wasteland cell now favors useful facings over ones pointing into more void. @done(26-08-22)
✔ #59 - orient: a no-op reorientation (same facing as current) is now rejected and randomOrientMove() won't generate one. @done(26-08-22)
✔ #60 - minion-picker buttons now read the piece's real orientation directly. @done(26-08-22)
✔ #61 - validateMinorPower: 3 "still declined" branches now behave like the existing 0-step case. @done(26-08-22)
✔ #62 - major arcana had the same "looks valid" gap as #61's minor-arcana fix; StepValidation now carries an explicit `complete` flag so validateMajorPower can tell a chain's last step is still being built. @done(26-08-22)
✔ #63 - tree-pruning: doomed choices (upright minion for Rods, a mode the hand can't complete, self-targeting tradeHands/hierophantReplace) are now struck through/rejected immediately with a specific reason. @done(26-08-22)
- #64 - better discard/draw messaging: both the basic action and the High Priestess version
- #65 - more consistent naming/numbering of major arcana cards, probably both in most circumstances
- #66 - test fool messaging on reload in production
✔ #67 - "Use Territory"/"Play Card" buttons could show the active card's slug too, not just which action started the move @done(26-09-08)
✔ #68 - an unresolvable bid tie (hands run dry) now ends the game with no winner, instead of falling back to an arbitrary turn order. @done(26-09-03)
✔ #69 - Edit messages; trimmed/reworded validation text and dropped translation-key mimicry from powers.ts's trusted-path backstop errors. @done(26-09-06)
✔ #70 - Improve Fool move strings; added a dedicated "continue" head word for resuming a paused power instead of re-typing use/play. @done(26-09-06)
✔ #71 - Address Fool corner cases: declaring mid-stride, etc. (nothing to draw at all, and no card left partway through, are both handled now - the former rejects use/play outright, the latter gracefully completes the Fool's use.) @done(26-09-06)
- #72 - Address the chatLog issue now in resolveChatSeat(), ideally on the next rebase onto develop.
✔ #73 - Fix dark mode; cards/wasteland cells now default to a transparent backdrop instead of opaque white. @done(26-09-06)
✔ #74 - Replaced "continue 00" move strings with an informative "play 9S (via 00)"/"decline 9S (via 00)" spelling, and extended to World. @done(26-09-08)
✔ #75 - Pass in and fully report card name in gnostica_territory. @done(26-09-08)
✔ #76 - Test reorienting a target minion. @done(26-09-09)
✔ #77 - provisionalResult no longer downgrades complete; each validate* computes it directly per head (place's "?", bid/redraw/discard fixed, chain/orient unconditional 0). @done(26-09-11)
- #78 - Add an Undo button to the button bar for backing out of various multi-step moves, e.g., a Rods/Discs/Swords "piece" step once a target has been chosen has no way to change it again.  Partly superseded in scope by #80.
✔ #79 - Simplification plan Phase 2: handlePendingStepBoardClick's "piece"-shape branch (Rods/Discs/Swords) briefly resolved its self-or-facing target through the shared pickPieceTargetClick primitive (same one tradeHands/orientAny/hierophantReplace/hermitTeleport already used), instead of its own duplicated inline self/face logic - itself superseded by #80 below. @done(26-09-09)
✔ #80 - Target selection for Rods/Discs/Swords "piece" mode is now button-only, not board-click: when the minion is facing another piece, buildStepModeMove leaves the target unset and getActionButtons offers a "target_<ref>" button per candidate (self, and whatever's at the facing cell) - mirroring the Swords pips-button precedent. Board clicks in "piece" shape are now unambiguous: purely about Rods' own distance (the full 1..size range, no more self-target collision to work around) and the trailing orientation once the target's own owner is the acting player. @done(26-09-09)
✔ #81 - Cups "own" now uses the exact same trailing-optional-orientation primitive as any other target minion: creation still always states a real, mandatory facing (rest[1], "U" by default - a brand-new minion can't reasonably go unoriented), but a further click sets an OPTIONAL 3rd token (rest[2]) that reorients it, hard-rejecting (ORIENT_NO_OP) a request matching rest[1] - the same no-op rule Rods/Discs/Swords already enforce, just compared against the peer creation-token instead of a board read (the piece doesn't exist yet to read). Dropped the old bespoke "still adjustable" soft-pedal and its DIRECTION_STILL_ADJUSTABLE messaging for this mode. @done(26-09-09)
✔ #82 - "place" (the very first piece) now writes its facing explicitly too, matching #81 - both of handleClickCore's own "place" branches (the very-first-click case, and the already-in-progress one) always include "U" or the clicked direction rather than omitting the token; validatePlace treats a genuinely missing orientation as still-building (complete:-1, DIRECTION_STILL_ADJUSTABLE) instead of silently defaulting it to "U". Removed cmdPlace's own "U" fallback too, once it was clear that was just a special case propping up ~240 trusted test call sites - every "place <cell>" in the test suite (gnostica.test.ts/gnostica.bidding.test.ts/gnostica.randomMove.test.ts) now spells out its orientation explicitly instead, no code special-cased around omitting it anywhere. Audited every other new-Piece call site (createOwn/createEnemy/growPiece/attackPiece/hierophantReplace) - all already require a real orientation, no other gaps found. @done(26-09-09)
✔ #83 - Simplification plan Phase 3 (partial): legalModesForMinion (randomMove's own pre-filter) no longer duplicates minorModeAvailability's per-mode legality switch byte-for-byte - it's now a thin wrapper that calls minorModeAvailability directly (narrowed to the {suitUid, minion, opts} it actually reads, so a real IPendingStep still satisfies it structurally) and discards the specific reason. @done(26-09-09)
✔ #84 - Separated the real recurring primitive underneath computeActionButtons' own button sets: "pick one value from a small labeled set" (ChoiceOption/buildChoiceButtons), used by all 8 of its independently hand-rolled loops (minion_/orientpick_/mode_/target_/pips_/hermit_/magician_/drawcount_+hpdraw_) instead of each re-deriving its own bolding/strikethrough logic. Found and fixed 2 genuine duplication bugs-in-waiting along the way: ROD_NEEDS_FACING was computed independently in 3 places (minorModeAvailability's own R.piece/R.tile case, the minion-picker's own button-strikethrough check, and the minion_ click dispatch's own rejection) - now one rodNeedsFacingReason helper feeds all 3, so button state and click rejection can no longer drift apart; drawcount_/hpdraw_'s identical count-picker loops are now one drawCountOptions helper. Deferred: the flow/priority order computeActionButtons dispatches states in was NOT redesigned this pass (evaluating that is easier now that each state's own button-building is already clean and isolated) - see #87. @done(26-09-09)
✔ #87 - Reviewed computeActionButtons' dispatch order: mostly necessary, not arbitrary (minion-disambiguation must precede special-kind dispatch; hermitTeleport/magicianChoice must escape the "no button set" branch) - left as is. Found one real duplication among the non-ChoiceSet states: the paused "Use Card X / Decline X" pair was built identically in 2 places - extracted to pausedPowerButtons(). The top-level bar itself had no duplication to extract. @done(26-09-10)
✔ #88 - Broke computeActionButtons up into its 8 named primitive states (isPlaceOnlyState/buildTopLevelBar/discardCountBar/orientAmbiguityBar/computePendingMinor/minionPickerBar/highPriestessCountBar/stepModeBar), which surfaced a real bug: worldUseAny and a suit-not-yet-chosen magicianChoice were still eagerly computing minion-ambiguity from before the "as" refactor, so 2+ minions on The World's/a fresh Magician's own cell wrongly showed a "Choose Minion" picker ahead of the card/suit choice (and picking one built a malformed move missing "as") - fixed in buildSpecialPending, with regression tests. Message composition (powerStepMessageKey) stays its own system - not folded in; that fuller merge remains a separate, bigger, deliberately out-of-scope leap. @done(26-09-10)
✔ #85 - Extracted randomMove() to a separate file for future deletion. @done(26-09-09)
- #86 - Once gnostica/randomMove.ts is deleted (or no longer needs them - see #85), revert these 28 gnostica.ts methods from public back to private (all were private before #85; only made public so randomMove.ts could call them cross-file): buildViaMove, cloneLive, computeShortcutOpts, eligibleMinionsForActivate, eligibleMinionsForPlay, hasPiecesOnBoard, minorModeAvailability, minorTargetCell, pickleMove, pieceRefStr, primitiveToSuit, renderWindow, scoreFor, targetScore, validateHermitStep, validateHierophantReplace, validateHighPriestess, validateJudgementDraw, validateMagicianChoice, validateMajorPower, validateMinorPower, validateOrientAny, validateOrientMinion, validateSuitPrimitive, validateTradeHands, victimRefStr, applyPowerStep, validatePowerStep. (chainMinion itself was reverted back to private static already - randomMove.ts keeps its own local copy instead, to avoid a circular import.) Also revert the IMinionRef/IStepOutcome/StepValidation/ALL_SUITS export/relocation changes #85 made if nothing else ends up needing them once randomMove.ts is gone.
- #89 - Manual live-server test: hand-mutate game state / the move string in a real game (e.g. via Emu Ranchers) and submit, to confirm the server rejects it cleanly and canonical game state stays intact. Not a code task or a liveMove concern.
- #90 - Replace this.liveMove (a stashed COPY of the raw parsed move string) with purpose-built fields render() actually reads, computed once inside move(..., {partial: true}) itself rather than re-derived from scratch by 5 different render-side functions every render() call. render()'s own signature is fixed by GameBase (just IRenderOpts - no move-string parameter, confirmed against _base.ts), so SOMETHING has to be stashed on the instance to bridge the handleClick -> move(partial) -> render() gap; the fix isn't eliminating that stash, it's stashing the right thing - the already-computed IPendingStep (feeds computeActionButtons' own pendingMinor construction directly, replacing parsePendingStep(pickleMove(this.liveMove))) plus whatever small explicit facts highlightedButtonValues/isPendingFirstPlacement/newHandCardUids need, matching Magnate's own this.highlights pattern - not a second copy of the move string, parsed or otherwise.
- #91 - The Fool either didn't let me do the second half of the Sun or was so obscure in its notation that I didn't realize I could; needs retesting.
✔ #92 - Replaced persisted pendingPower (whole IPowerFrame[] stack with minions/viaFool) with a minimal continued: string[] token stack. @done(26-09-09)
✔ #93 - High Priestess round 2 now resumes as a bare "discard <uids> draw <n> (via 02)" (its step IS a discard/draw). buildViaMove/describePendingMove emit the "discard" head + tokens for a highPriestess pending; resumeStepSegments() folds "discard"-head tokens back into a step segment for the shared resume machinery; pickleMove drops rest[0] when it equals viaUid (keeps pickle(parse(x)) stable now that parseMove folds the anchor into rest[0]). @done(26-09-09)
✔ #94 - validateMove now requires a resume submission to spell the head that fits the step (validateResumeHead): "decline"/"discard"(High Priestess)/"play"(everything else), plus a matching "(via <root>)" anchor - no more head-is-decorative leniency for untrusted callers. A wrong anchor is PENDING_POWER_MISMATCH, a wrong head INVALID_MOVE. move() (trusted path) stays lenient per feedback_no_trusted_path_defense. validateResumePendingPower lost its now-redundant root-uid check and param. @done(26-09-10)
- #94 - Remove one-liner wrappers like invalid().
- #95 - Investigate cloneLive().
✔ #96 - WORLD_CHOOSE_TARGET no longer tells the player to "click Decline" - a fresh World activation has no Decline button (#49). @done(26-09-10)
- #97 - Bonus highlighting:  of major arcana cards on the board during the World's pick, of own minion cells for a play <carduid>, etc.
